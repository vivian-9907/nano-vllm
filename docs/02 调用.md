```text
example.py
  │
  ├─ llm = LLM(...)
  │     └─ 创建 Scheduler
  │
  └─ llm.generate(prompts, sampling_params)
        │
        ├─ scheduler.add(seq)          请求进入 waiting 队列
        │
        └─ 循环调用 llm.step()
              │
              ├─ scheduler.schedule()  选出本轮要计算的请求
              │
              ├─ model_runner.run()    模型真正执行
              │
              └─ scheduler.postprocess()
                                        更新请求状态
```
